---
description: 新建 Java 类型须含类 Javadoc、@author（git user.name）、@date（创建时间）；bubble-api-oa / bubble-biz-oa 全量 Java 类须同时具备 @author 与 @date；实体对齐 Admin.java（@Schema 字段注释）
globs: "**/*.java"
alwaysApply: true
---

# Java 类文件头注释（全模块）

对所有模块中**新创建**的 Java 源文件，在**类型声明**（`class` / `interface` / `enum` / `record`）**正上方**必须包含如下结构的 Javadoc（第一行为对该类型的简短中文说明，勿留空）：

```java
/**
 * 此处为类型说明（如：OA 模块启动类、用户登录请求 DTO）
 *
 * @author <Git 用户名>
 * @date <创建时间>
 */
```

## @author

- 填写**当前仓库** `git config user.name` 的值；若未设置，则使用 `git config --global user.name`。
- **禁止**写死示例中的占位姓名；新建文件时若不确定，应先根据上述命令读取再写入。
- 若环境无任何 git 用户配置，则使用 `Unknown` 并避免编造真实人名。

## @date

- 填写**创建该文件时**的本地日期时间（新建文件当下时刻，勿使用历史示例日期）。
- 推荐格式二选一（与团队约定保持一致即可）：
  - 中文习惯：`2026/3/28 下午2:57`（12 小时制 + 上午/下午）；
  - 或 24 小时制：`2026/3/28 14:57`。
- **禁止**复制粘贴旧文件中的 `@date` 而不更新。

## 适用范围

- 适用于**新文件**中第一个顶层类型；同一文件内若新增第二个顶层类型，该类型同样需带完整头注释。
- **不要**为仅修改的已有文件强制补写（除非用户明确要求统一补全）。

## OA 模块（`bubble-api-oa` / `bubble-biz-oa`）

- 上述两个模块内**每一个** `.java` 源文件的**第一个顶层类型**的 Javadoc 中必须**同时**包含 `@author` 与 `@date`（缺一不可）。
- `@author` 取值规则同上文（`git config user.name`）。
- 批量补历史文件时，允许使用统一补全时的同一 `@date`；**新建文件**仍须使用创建时刻，禁止照抄无关旧日期。

## 与现有代码的关系

- 与 `backend-conventions.mdc` 中的格式、缩进等规范同时遵守；本规则优先约束**类上 Javadoc 结构**。

## Bubble-API `entity` 实体类（与 `Admin.java` 一致）

对 `bubble-api/bubble-api-oa`（及同模块风格）中 **MyBatis-Plus 实体**（`extends Req<T>`、`@TableName`），须与 `bubble-api-oa` 下 `com.bubblecloud.oa.api.entity.Admin` 保持同一套注释与注解方式：

- **类上 Javadoc**（第一行中文业务说明，并写明物理表名）  
  - 格式：`/**` + 换行 + ` * <业务说明>，对应 <eb_xxx> 表。` + ` */`  
  - 示例见：`Admin.java` 中 `员工账号，对应 eb_admin 表。`

- **类上 Swagger**  
  - `@Schema(description = "<中文短名>")` 与 `Admin` 一致，便于 OpenAPI 展示。

- **字段**  
  - **每个**非 `serialVersionUID` 的持久化字段必须带 `@Schema(description = "...")`，说明为**中文**（含义、枚举取值说明可写在括号内）  
  - 与 MyBatis-Plus 注解并存：主键 `@TableId`、特殊列名 `@TableField` 等写在字段上，`@Schema` 紧贴字段声明（与 `Admin` 相同顺序）。

- **`@author` / `@date`**  
  - 与 OA 模块全量要求一致：实体类同样必须在类 Javadoc 中写明 `@author` 与 `@date`（与 `Admin.java` 当前形态一致）。

- **参考文件**  
  - `bubble-api/bubble-api-oa/src/main/java/com/bubblecloud/oa/api/entity/Admin.java`
