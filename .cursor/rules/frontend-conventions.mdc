---
description: Vue 3 前端开发规范，涵盖 Bubble-UI 项目的编码约定、组件规范和 API 集成方式
globs: "apps/bubble-ui/**/*.{vue,ts,tsx,js}"
alwaysApply: false
---

# 前端开发规范 (Bubble-UI)

## 技术栈

- Vue 3.5 + `<script setup>` + TypeScript（strict 模式）
- Element Plus 2.13 + Tailwind CSS 3.4 + SCSS
- Vite 5.4 构建，路径别名 `/@/*` → `src/*`
- Pinia 状态管理 + pinia-plugin-persist
- Axios HTTP 客户端，封装于 `src/utils/request.ts`
- vue-i18n 国际化，vue-router Hash 模式

## 目录职责

```
src/
├── api/{module}/      → REST API 封装，按后端模块划分(admin/agi/daemon/gen/login)
├── components/        → 全局通用组件（上传、权限、编辑器、分页等）
├── views/{module}/    → 页面组件，与 api/ 模块对应
├── stores/            → Pinia store 模块（主题、用户、路由、字典等）
├── router/            → 路由定义与守卫（后端菜单驱动为默认模式）
├── layout/            → 主布局框架与多布局变体
├── hooks/             → 组合式函数封装（message/table/dict）
├── utils/             → 工具函数（请求、存储、加密、校验等）
├── i18n/              → 全局与页面级语言包
├── theme/             → 全局样式、暗色模式、Element 覆盖
├── directive/         → 自定义指令（权限等）
├── types/             → TypeScript 全局类型声明
└── assets/            → 静态资源（SVG 图标、图片等）
```

## 路由机制

- 默认使用**后端菜单控制路由** (`themeConfig.isRequestRoutes: true`)
- `backEnd.ts`: 调用 `getAdminMenu()` 获取菜单，`import.meta.glob` 动态映射组件
- `frontEnd.ts`: 备选的前端角色过滤方案
- 静态路由: `/login`、`/401`、`/404`

## API 调用规范

- 统一使用 `/@/utils/request` 导出的 axios 实例
- baseURL 由 `VITE_API_URL` 环境变量控制
- 请求自动附加 `Bearer` token，支持 `skipToken` 跳过
- 微服务/单体适配: `VITE_IS_MICRO` 控制 URL 路径转换
- 响应 `code === 1` 为业务失败，`status 424` 触发重新登录
- 支持请求/响应加密 (`Enc-Flag` + `VITE_PWD_ENC_KEY`)

## 关键环境变量

| 变量 | 用途 |
|------|------|
| `VITE_API_URL` | API 基础地址 |
| `VITE_ADMIN_PROXY_PATH` | 开发代理目标 |
| `VITE_PORT` | 开发服务器端口 |
| `VITE_PUBLIC_PATH` | 生产 base path |
| `VITE_IS_MICRO` | 是否微服务模式 |
| `VITE_PWD_ENC_KEY` | 密码加密密钥 |
| `VITE_OAUTH2_PASSWORD_CLIENT` | OAuth2 密码客户端 |
| `VITE_OAUTH2_MOBILE_CLIENT` | OAuth2 手机客户端 |

## 新增页面检查清单

1. `src/views/{module}/` 下创建页面组件
2. `src/api/{module}/` 下创建对应 API 封装
3. 后端 `sys_menu` 表插入菜单/按钮权限数据
4. 若需国际化，在 `src/i18n/pages/` 或页面 `i18n/` 子目录添加语言包
5. 权限按钮使用 `<auth>` 组件或 `v-auth` 指令包裹
