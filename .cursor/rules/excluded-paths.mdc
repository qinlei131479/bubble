---
description: 定义不应纳入 AI 分析、搜索和记忆的排除路径，避免处理构建产物和依赖
alwaysApply: true
---

# 排除路径规则

以下目录和文件在开发辅助中应**始终排除**，不纳入代码搜索、分析和上下文记忆。

## 必须排除的目录

| 路径模式 | 原因 |
|---------|------|
| `**/target/` | Maven 编译产物，自动生成 |
| `apps/**/node_modules/` | npm 依赖包，体积大且自动安装 |
| `apps/**/dist/` | 前端构建输出产物 |
| `**/.git/` | Git 仓库元数据 |
| `**/.idea/` | IntelliJ IDEA 项目配置 |
| `**/.vscode/` | VSCode 编辑器配置 |
| `**/logs/` | 运行时日志文件 |
| `**/.flattened-pom.xml` | Maven flatten 插件产物 |
| `**/.DS_Store` | macOS 系统文件 |
| `**/*.iml` | IntelliJ 模块文件 |
| `**/*.iws` / `**/*.ipr` | IntelliJ 工作区/项目文件 |
| `**/__pycache__/` | Python 编译缓存（预留 apps/python） |
| `**/.venv/` | Python 虚拟环境（预留 apps/python） |

## 不应修改的生成文件

- `**/git.properties` — git-commit-id 插件自动生成
- `apps/bubble-ui/auto-imports.d.ts` — unplugin-auto-import 自动生成
- `apps/bubble-ui/components.d.ts` — 组件自动导入声明

## 注意事项

- 搜索代码时使用 `--glob '!**/target/**' --glob '!**/node_modules/**'` 排除
- 分析项目结构时跳过上述目录
- 不要将这些路径中的内容作为代码修改建议的依据
