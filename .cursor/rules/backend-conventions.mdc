---
description: Java 后端规范：Spring Cloud 分层与注解、OA 迁移（含 UpService/UpMapper）、REST 与依赖；类 Javadoc、OA 实体与 Admin.java 对齐
globs: "**/*.java"
alwaysApply: true
---

# 后端开发规范

## 代码格式

- Spring Java Format（`spring-javaformat-maven-plugin 0.0.47`），`mvn spring-javaformat:apply`
- 缩进：Tab、宽 4、续行 8（`.editorconfig`），UTF-8

## 模块分层

```
bubble-api-{module}      → 实体、DTO/VO、Feign（jar，不可独立运行）
bubble-biz-{module}      → Controller / Service / Mapper（可部署）
bubble-common-{feature}  → 跨模块组件
bubble-visual-{feature}  → 可视化管理服务
```

## 新增模块

1. 父 POM `<modules>` 注册  
2. `bubble-common-bom` 版本管理  
3. 可部署服务：`application.yml` + `logback-spring.xml` + `Dockerfile`  
4. 启动类：`@EnableDoc("模块名")` + `@EnableCustomFeignClients` + `@EnableCustomResourceServer`  
5. Nacos：`{spring.application.name}-{profile}.yml`

## 自动配置

- Boot 3：`META-INF/spring/org.springframework.boot.autoconfigure.AutoConfiguration.imports`  
- Feign：`META-INF/spring/org.springframework.cloud.openfeign.FeignClient.imports`

## 关键注解

| 注解 | 用途 | 模块 |
|------|------|------|
| `@EnableCustomResourceServer` | OAuth2 资源服务器 | common-security |
| `@EnableCustomFeignClients` | Feign 扫描 | common-feign |
| `@EnableDoc("name")` | SpringDoc | common-swagger |
| `@EnableDynamicDataSource` | 多数据源 | common-datasource |
| `@Inner` | 内部调用免鉴权 | common-security |
| `@SysLog("描述")` | 操作日志 | common-log |
| `@HasPermission` | 方法权限 | common-security |

## 实体

- 一般继承 `BaseEntity`（审计字段由 `MybatisPlusMetaObjectHandler` 填充）；JSON 数组用 `JsonStringArrayTypeHandler` / `JsonLongArrayTypeHandler`
- **OA（`bubble-api-oa`）**：迁移表实体继承 `Req<T>`，与 `eb_*` 对齐，**不**用 `BaseEntity`；类 Javadoc、`@Schema`、字段注释见下文 **OA 实体与 `Admin.java`**

## 统一返回

- `R<T>`；成功 `R.ok(data)`，失败 `R.failed(msg)` 或 `R.failed(ErrorCodes.XXX)`；错误码与 `i18n/messages` 配套
- **OA（`bubble-biz-oa`）**：与 PHP 前端对齐时使用 `R.phpOk(data)` / `R.phpFailed(msg)`（`msg` 成功侧为 `ok`，载荷在 `data`）；JSON 中额外包含只读 `status`（200/400），与 `code`（0/1）并行，**勿**再使用已移除的 `PhpResponse`

## 依赖

- 版本由 `bubble-common-bom` 管理，业务模块勿硬编码；Web 用 Undertow；Feign 用 OkHttp

## 工具类选用顺序（`bubble-biz-oa` 及推荐全后端）

**优先级**：**HuTool（`cn.hutool`）** → **`bubble-common-core` 等工程内封装**（如 `HuToolUtil`）→ **`org.springframework.util`**（仅在前两者无等价能力时使用）。

| 场景 | 推荐 | 避免 |
|------|------|------|
| 字符串是否非空（原 `StringUtils.hasText`） | `cn.hutool.core.util.StrUtil`：`StrUtil.isNotBlank` / `StrUtil.isBlank` | `org.springframework.util.StringUtils.hasText` |
| 对象是否为 `null`、是否非 `null` | `cn.hutool.core.util.ObjectUtil`：`ObjectUtil.isNull` / `ObjectUtil.isNotNull` | 裸写 `== null` / `!= null` |
| 包装数字「有值且非 0」（如 `x != null && x != 0`） | `ObjectUtil.defaultIfNull(x, 0) != 0` 或与语义一致的 `NumberUtil` | 冗长的 `!= null && != 0` 链（易重复求值） |
| 默认值、判空集合等 | 优先 HuTool `ObjectUtil` / `CollUtil` / `NumberUtil` 等与场景匹配的 API | Spring `CollectionUtils` 等，除非 HuTool 无对应方法 |

- 新增或修改 `bubble-biz/bubble-biz-oa` 代码时按上表执行；历史代码可随需求逐步对齐。

---

## bubble-biz-oa（迁移）

- **命名**：与 `eb_*` 表一致，无 `Oa` 前缀（如 `eb_admin` → `Admin` / `AdminMapper` / `AdminService`）
- **禁止**无表空壳 `Bootstrap`/`Facade`；能力落在对应 `XxxService`
- **禁止**业务层 `Map<String, Object>`（入参、出参、Mapper 行）；用 `bubble-api-oa` 的 DTO/VO/实体或行视图。**例外**：JWT、第三方 SDK、内部解析；PHP 扁平动态 JSON 用 `JsonNode`/`ObjectNode`，勿用 `Map` 作动态表单；键值列表用 `List<XxxItemVO>`
- **SQL**：自定义 SQL 写在 `bubble-biz-oa/.../mapper/XxxMapper.xml`；接口上**禁止** `@Select`/`@Update` 等（BaseMapper/lambda 无 XML 的 CRUD 除外）
- **Service**：入参 `*DTO`，返回 `*VO` 或已有实体；禁止 `JdbcTemplate` 直查 OA 表
- **对外兼容**：统一 `com.bubblecloud.common.core.util.R<T>`（`R.phpOk`/`R.phpFailed` + 序列化 `status`/`code`/`msg`/`data`）
- **阶段**：见 `docs/plans/oa-migration-plan.md`；**禁止**新增 `SimplePageVO.empty` 类占位桩

### Service 与 Mapper（`UpService` / `UpMapper`）

- **`XxxService`**：必须 `extends UpService<实体>`（实体与表/`eb_*` 一一对应，泛型随业务而变，勿写死为某一表名）
- **`XxxServiceImpl`**：必须 `extends UpServiceImpl<XxxMapper, 实体> implements XxxService`（参考 `AdminService` / `AdminServiceImpl`）
- **`XxxMapper`**：必须 `extends UpMapper<实体>`，并加 `@Mapper`（参考 `AdminInfoMapper`）
- **方法扩展**：优先使用 `UpService`、`UpMapper` 已有通用方法（含 lambda 条件封装）；仅在基类无法满足时再新增接口方法或 `XxxMapper.xml` 自定义 SQL

### Controller

- **禁止**注入 Mapper、在 Controller 写 `Wrappers.lambdaQuery`；经 `XxxService` → Mapper/XML
- **风格**：可参考 `bubble-biz-agi` 的 `AgentConfigsController`；**优先级**：PHP 路由与字段 > Java 通用写法
- **OA**：Controller 只做路由、参数绑定、调用 `XxxService`，**不写业务分支**（校验、聚合、异常语义在 `XxxServiceImpl`）；返回 `R.phpOk` / `R.phpFailed` 或依赖全局异常转为 `R.phpFailed`；勿为对齐 AGI 改 PHP 路径
- **分页**：PHP 的 `current`/`size`/`page`/`limit` 等与 `Pg` 不一致时在 **Controller** 装配 `Pg`（`setCurrent`/`setSize`）或 `@ParameterObject Pg`，再调 `findPg`；**ServiceImpl** 按需重写 `findPg`；对应 **`XxxMapper.xml` 必须实现带分页的 `findPg` SQL**（动态条件写在 XML）
- 权限 `@HasPermission`，写操作可 `@SysLog`

#### CRUD 方法命名（`bubble-biz-oa` 推荐与 `bubble-biz-agi` 对齐）

| 语义 | Java 方法名 | 典型映射（可按 PHP 再并列 `value = {"", "/page"}` 等） |
|------|-------------|--------------------------------------------------------|
| 分页列表 | `page` | `@GetMapping({ "", "/page" })` + `UpService.findPg(Pg, Req 实体)` |
| 按 id 详情 | `details` | `@GetMapping("/{id}")` 或 `/{id}/edit`；**单表**可直接 `getById` |
| 新增 | `create` | `@PostMapping` + `save`/`create`（基类） |
| 修改 | `update` | `@PutMapping` + `UpService.update(req)` 或 `update(req, isCustom)` |
| 按 id 删除 | `removeById` | `@DeleteMapping("/{id}")` + `removeById`/`remove` |

- **GET 新增表单占位**（PHP 常配 `/create`）：方法名用 **`createForm`**，避免与 **`create`（POST 新增）** 同名。
- **非分页列表**（树、全量）：方法名用业务语义，如 **`list`**、**`workbench`** 等，**不要**占用 `page`。
- **其它业务接口**（状态切换、树、导出等）在 Controller 中**另起方法**；能复用 **`UpService`/`findPg`/`getById`** 的优先复用。
- **自定义更新 SQL**：在 **`XxxServiceImpl`** 中调用 **`update(T req, boolean isCustom)`**，`isCustom == true` 时走 **`XxxMapper.xml` 的 `updateCustom`**（见 `UpServiceImpl`）。

---

## 类 Javadoc（全模块 Java）

每个**顶层类型**（`class` / `interface` / `enum` / `record`）声明**正上方**须有 Javadoc：首行**中文**简述类型职责，勿空；并含：

```java
/**
 * 类型说明（例：OA 启动类、登录请求 DTO）
 *
 * @author <git config user.name，先 local 再 global>
 * @date <创建时刻的本地时间>
 */
```

- **@author**：仓库 `git config user.name`，否则 `--global`；全无则用 `Unknown`，勿编造
- **@date**：**创建该文件时**的时间，勿抄旧文件；格式任选：`2026/3/28 下午2:57` 或 `2026/3/28 14:57`
- **范围**：**新建**文件（及同文件新增的第二顶层类型）必写；仅改旧文件不强制补（除非用户要求批量统一）
- **`bubble-api-oa` / `bubble-biz-oa`**：每个 `.java` 第一个顶层类型的 Javadoc **必须同时**含 `@author` 与 `@date`；批量补历史可共用同一 `@date`，新建仍用创建时刻

## OA 实体与 `Admin.java`（`bubble-api-oa` MyBatis-Plus 实体）

与 `com.bubblecloud.oa.api.entity.Admin` 一致：

- 类 Javadoc 首行：`业务说明，对应 eb_xxx 表。`；`@Schema(description = "中文短名")`
- 除 `serialVersionUID` 外每个持久化字段 `@Schema(description = "中文…")`；与 `@TableId` / `@TableField` 等顺序同 `Admin`
- 类 Javadoc 含 `@author`、`@date`

参考：`bubble-api/bubble-api-oa/src/main/java/com/bubblecloud/oa/api/entity/Admin.java`
