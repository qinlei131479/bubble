<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bubblecloud.biz.oa.mapper.DictDataMapper">

	<select id="findPg" resultType="com.bubblecloud.oa.api.entity.DictData">
		SELECT t.*
		FROM eb_dict_data t
		<where>
			<if test="query.name != null and query.name != ''">
				AND t.name LIKE CONCAT('%', #{query.name}, '%')
			</if>
			<if test="query.typeName != null and query.typeName != ''">
				AND t.type_name = #{query.typeName}
			</if>
			<if test="query.typeId != null">
				AND t.type_id = #{query.typeId}
			</if>
			<if test="query.status != null">
				AND t.status = #{query.status}
			</if>
		</where>
		ORDER BY t.sort DESC, t.id DESC
	</select>

</mapper>
