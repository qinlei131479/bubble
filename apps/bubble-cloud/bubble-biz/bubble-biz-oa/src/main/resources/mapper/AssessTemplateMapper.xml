<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bubblecloud.biz.oa.mapper.AssessTemplateMapper">

	<select id="findPg" resultType="com.bubblecloud.oa.api.entity.AssessTemplate">
		SELECT t.*
		FROM eb_assess_template t
		<where>
			AND t.deleted_at IS NULL
			<if test="query.entid != null">
				AND t.entid = #{query.entid}
			</if>
			<if test="query.types != null and query.types != ''">
				AND t.types = #{query.types}
			</if>
			<if test="query.isFavorite != null">
				AND t.is_favorite = #{query.isFavorite}
			</if>
			<if test="query.status != null">
				AND t.status = #{query.status}
			</if>
		</where>
		ORDER BY t.id DESC
	</select>

</mapper>
