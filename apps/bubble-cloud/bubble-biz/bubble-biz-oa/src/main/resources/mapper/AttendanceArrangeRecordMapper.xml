<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bubblecloud.biz.oa.mapper.AttendanceArrangeRecordMapper">

	<select id="findPg" resultType="com.bubblecloud.oa.api.entity.AttendanceArrangeRecord">
		SELECT t.*
		FROM eb_attendance_arrange_record t
		WHERE t.deleted_at IS NULL
		ORDER BY t.id DESC
	</select>

	<update id="logicDeleteFutureByShiftId">
		UPDATE eb_attendance_arrange_record
		SET deleted_at = NOW(), updated_at = NOW()
		WHERE deleted_at IS NULL
		  AND shift_id = #{shiftId}
		  AND DATE(`date`) &gt; #{afterDate}
	</update>

</mapper>
